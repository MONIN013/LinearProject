# 実験反復を優先する作業規約

- 通常は変更箇所に直結する最小の検証を1回だけ実施する。同じコード・設定で確認済みのテストを繰り返さない。
- 軌道・FF係数の調整だけなら全件テストを実行しない。モデル・周期・固定バッファ構成と実機モジュールが一致していれば、再ビルド・再Activateも不要。
- コード変更時は該当関数のテスト、モデル変更時は該当モデルのチェックと必要なビルドに絞る。全件テストは明示依頼または共通基盤の広範な変更時に限る。
- 文書変更やテスト関数の削除だけなら差分確認で済ませ、MATLABを起動しない。必要なMATLAB検証は可能な限り1セッションにまとめる。
- 無関係なテスト失敗やライセンスによるskipを実験の前提にしない。原因に関係する変更・新しい根拠なしに再試行しない。
- 停止処理、電流上限、通信異常時の保護、実験前後の実機状態確認は省略しない。制御器やプラントを変更した場合は実際に使用する組合せの安定性・感度を確認する。
- 実験中は計測欠落・追従・終了後の無励磁を確認し、失敗した実験を成功扱いしない。テスト高速化のために実機の待機時間や安全ゲートを削らない。
- 仮想CSTはSimulinkから1本のトルク指令を出し、実軸選択・磁極参照・鎖交区間はtwincat/MotorRuntimeが担当する。
